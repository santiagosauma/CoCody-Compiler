mi_lista <- [5, 10, 15, 20, 25] FIN
i <- 0 FIN
mientras (i < 5) HACER
    si (mi_lista[i] % 2 == 0) ENTONCES
        muestra("Valor par encontrado") FIN
    FIN_SI
    muestra(mi_lista[i]) FIN
    i <- i + 1 FIN
FIN_MIENTRAS